// بسم اللہ الرحمان الرحیم

#include <cmath>              // {10} sin, cos, tan, abs, sqrt, cbrt, pow, ceil, floor, round
#include <ctime>              // {-}  time; or use <chrono> (newer, better)
#include <chrono>             // {-}
#include <cctype>             // {8}  isalpha, isdigit, isupper, islower, isspace, ispunct, toupper, tolower
#include <random>             // {4}  random_device, mt19937, uniform_int_distribution, uniform_real_distribution; for random nums instead of rand();
#include <string>             // {15} string, at, size, capacity, shrink_to_fit, append, insert, erase, replace, substr, find, stoi, stoll, stod, to_string
#include <thread>             // {2}  sleep_for, sleep_until
#include <vector>             // {15} at, size, capacity, reserve, shrink_to_fit, begin, end, push_back, insert, pop_back, erase, clear, empty, front, back
#include <cstdlib>            // {3}  rand, srand, exit; use <random> instead; 
//! #include <fstream>        // for filing
#include <iomanip>            // {7}  left, right, setw, setprecision, setfill, boolalpha, noboolalpha
#include <numeric>            // {1}  accumulate
#include <utility>            // {1}  pair (struct of only 2 items)
#include <iostream>           // {10} cin, get, getline, ws, good, fail, clear, ignore, cout, endl
#include <algorithm>          // {7}  find, binary_search, sort, stable_sort, is_sorted, count, reverse
using std::cin, std::cout;    // only works in C++17+

/*
    [ToDo]
        (1) Learn Raylib;
        (2) Prac Project:    Tic Tac Toe with Raylib;
        (3) OOP Lab Project: Space Shooter with Raylib; 
        inshaAllah
*/


int main(){

}